import {
  Card,
  CardActions,
  CardContent,
  CardHeader,
  Grid,
  Typography,
  Button,
  Paper,
} from "@mui/material";
import nextConfig from "next.config.mjs";

const Home = () => {
  return (
    <Grid container padding={0} rowSpacing={5} columnSpacing={0}>

    </Grid>
  );
};

export default Home;
